import React from 'react';
import ic_edit from '../assets/ic_edit.svg';

const EditIcon = () => {
    return (
        <div>
           <img src={ic_edit} width={20} height={20} />
        </div>
    );
};

export default EditIcon;
