import React from 'react';
import ic_save from '../assets/ic_save.svg';

const SaveIcon = () => {
    return (
        <div>
            <img src={ic_save} width={20} height={20} />
        </div>
    );
};

export default SaveIcon;
